# TypeScriptFoundry Project Template

Use this folder as the starting point for a new independently runnable TypeScript project.

## Setup

```bash
npm install
npm run build
```

## Customize

Rename the package metadata and replace `src/index.ts` with the project implementation.
